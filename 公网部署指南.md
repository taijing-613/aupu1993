# 把网站搬到公网 — Railway 傻瓜部署指南

> 目标：让任何人在任何地方打开一个 `https://xxx.up.railway.app` 网址就能用这个系统，
> **不再依赖你的电脑开着**。
>
> 前置条件已满足：代码已在 GitHub 公开仓库 `taijing-613/aupu1993`，
> 部署配置（Dockerfile / railway.json / requirements.txt）已备好且已修正路径错误。

---

## 开始之前：唯一需要你手动做的事（30 秒）

部署所需的全部配置都已用代码方式写好并**提交到本地**了，只差推到 GitHub。
（我这边有网络但没有你的 GitHub 凭据，不能替你推送。）

1. 打开 **GitHub Desktop**
2. 左下角填提交说明：`fix: 根目录 Dockerfile 支持自动构建`
3. 点 **Commit to master**
4. 右上角点 **Push origin**（推送）
5. 去 https://github.com/taijing-613/aupu1993 确认能看到这次提交
6. **Railway 会自动重新部署**，回到 Railway 页面看构建日志即可

> 本指南后续步骤（挂卷 / 配 Key / 查网址）都是可选的优化项，
> 推送成功且构建通过后，网站就已经能公网访问了。

---

## 第 1 步：注册 Railway

1. 打开 https://railway.app
2. 点 **Login** → 选 **Login with GitHub**（用你刚推代码的那个账号 `taijing-613`）
3. 在弹出的授权页点 **Authorize Railway**

> 首次注册会送 **$5 试用额度，不需要绑信用卡**。

---

## 第 2 步：创建项目并连接仓库

1. 点右上角 **New Project**
2. 选 **Deploy from GitHub repo**
3. 如果首次使用，会提示安装 Railway 到 GitHub：点 **Install & Authorize**，
   建议选 **Only select repositories** → 勾选 `aupu1993` → **Install**
4. 回到 Railway，在列表里点选 **`aupu1993`**

此时它会自动开始第一次构建，**大概率会失败**（因为还没设根目录），别慌，下一步解决。

---

## 第 3 步：设置根目录（通常已不需要 ✅）

**好消息：这一步已经用代码方式解决了。**

仓库根目录现在有了一个专门的 `Dockerfile`，它会自己把 `app/` 目录的内容
复制进镜像，所以 Railway 在根目录就能自动构建，**不需要再填 Root Directory**。

> 之前失败（日志出现 `Railpack`、列出根目录那些 .md 文件）就是因为根目录
> 没有 Dockerfile，Railway 用了自动构建器，扫到根目录找不到可启动的应用。

**只有在仍然报 `requirements.txt: No such file or directory` 时**才手动补：

1. 点开服务卡片 → **Settings（中文界面可能显示为「背景设定」）**
2. 找 **Source** 区块的 **Root Directory**
3. 填入 `app` → 保存

---

## 第 4 步：配置环境变量

仍在 **Settings** → 找 **Variables** 区块（或顶部 **Variables** 标签）：

| 变量名 | 值 | 说明 |
|---|---|---|
| `DATA_DIR` | `/app/data` | 数据库和上传图片的存放目录（配合下一步的持久化卷） |
| `VISION_API_KEY` | 你的 OpenAI Key（选填） | 填了才能启用真正的 AI 视觉对比；不填则自动走旧的像素比对兜底 |
| `PORT` | **不要手动填** | Railway 会自动注入，填了反而可能冲突 |

填法：点 **+ New Variable** → 左边填名字，右边填值 → 自动保存。

---

## 第 5 步：挂一个持久化卷（不挂会丢数据）

Railway 的容器重启后文件会清空，必须挂卷，否则你登记的成员、巡检记录、上传的凭证图都会丢。

1. 在服务页面点 **Settings**
2. 找 **Volumes** 区块 → 点 **+ Add Volume**
3. **Mount Path**（挂载路径）填：

```
/app/data
```

4. 点 **Add / Deploy**

> 注意：免费额度的卷容量上限是 0.5 GB。巡检图和凭证截图会占空间，建议定期在
> 网站「数据报表」页导出 CSV 备份。

---

## 第 6 步：拿到公网网址

1. 服务页面切到 **Settings** → 找 **Networking** 区块
2. 点 **Generate Domain**
3. 稍等几秒，会出现一个网址，形如：

```
https://aupu1993-production-xxxx.up.railway.app
```

4. **复制它** —— 这就是你的公网网站地址，发给任何人都能直接打开。

> 想换成好记的域名：在 Networking 里点 **Custom Domain**，按提示去你的域名商加一条 CNAME 即可（非必需）。

---

## 第 7 步：验证网站能用

1. 浏览器打开刚拿到的网址
2. 会弹出**门禁登记**（姓名 + 部门）→ 填一下进入
3. 点顶部 **扫码登录** → 用手机淘宝扫码
   - 二维码是**服务器端**生成的，你手机扫的是服务器上的浏览器，完全正常
   - 登录态保存在服务器的 `/app/data` 里（已挂卷，不会丢）
4. 随便发起一次巡检，确认能出结果

---

## ⚠️ 三个你必须知道的坑

### 1. 内存：免费额度大概率会 OOM（最重要）

这个系统用 **Playwright + Chromium 浏览器内核**去真实抓取淘宝页面，
Chromium 一开就吃掉 **300–600 MB 内存**，加上 Python 和 gunicorn，很容易超过
Railway 免费档的 **0.5 GB 上限** → 表现为**服务反复重启 / 抓图到一半崩溃**。

建议：
- 只是**演示一天两天**：先用 $5 试用额度跑，够用
- 要**长期稳定用**：升级 **Hobby 计划 $5/月**（含 $5 额度，资源上限放宽到数 GB），
  这是最省心的选择

判断方法：在 Railway 服务页的 **Metrics** 看内存曲线，长期贴顶就是内存不够。

### 2. 数据不互通

公网上的实例是**全新的空数据库**，你本地 `localhost:8000` 里已有的巡检记录、
店铺、标准图**不会自动同步**过去。需要的话：
- 在本地网站「数据报表」页导出 CSV → 到公网网站重新录入
- 或者干脆把公网那份当作正式环境，从零开始用

### 3. 代码更新会自动重新部署

以后你（或同事）往 GitHub 推代码，Railway 会**自动重新构建部署**。
不想自动更新可以在 Settings 里关掉 **Auto Deploy**。

> 小提示：Railway 已宣布 `railway.json` 这类「配置即代码」方式将于
> **2026-12-01** 后停用。按本指南在**仪表盘里手动设置**的效果完全一样，
> 且不受该变更影响。

---

## 常见问题对照表

| 报错 / 现象 | 原因 | 怎么解决 |
|---|---|---|
| `requirements.txt: No such file or directory` | Root Directory 没设成 `app` | Settings → Root Directory 填 `app` |
| 构建很慢（5–10 分钟）卡住 | 正常，要装 Chromium 内核 | 耐心等，别重复触发部署 |
| 部署成功但打不开 / 502 | 启动命令或 PORT 冲突 | 确认没手填 PORT；Start Command 留空用 Dockerfile 默认 |
| 用一会儿就重启、抓图崩溃 | 内存超 0.5 GB | 升级 Hobby 计划，或减少并发巡检数 |
| 数据过一会儿全没了 | 没挂卷，或挂载路径不是 `/app/data` | 按第 5 步挂卷 |
| 扫码登录一直转圈 | 登录态过期或服务器 IP 被淘宝风控 | 重试扫码；若持续失败属反爬限制，属已知 P2 问题 |
| AI 视觉对比没生效 | 没配 `VISION_API_KEY` | 按第 4 步填 Key，再到网站「项目设置」开启并点视觉自检 |

---

## 备选方案：Render（免费但不推荐）

如果实在不想花钱：
- https://render.com → New → Web Service → 连 GitHub 仓库
- Root Directory 同样填 `app`，Runtime 选 **Docker**
- 免费档**会在 15 分钟无访问后休眠**，下次打开要等 30–50 秒冷启动；
  且内存只有 512 MB，对 Playwright 来说更吃紧

**结论：要长期给同事/客户用，Railway Hobby $5/月 是性价比最高的选择。**

---

## 部署完成后

- ✅ 网站 24 小时在线，不依赖你的电脑
- ✅ 任何人打开网址即可使用
- ✅ 以后推代码自动更新

把这个网址发给你要分享的人即可。
