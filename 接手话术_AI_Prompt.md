# 接手话术（给同事）

用途：把下面 **引用块内** 的内容整段复制，粘贴给新会话的 AI（如 WorkBuddy / ChatGPT / Claude），它就能快速接手并继续推进本项目。

> 你是一名资深全栈工程师，现在接手"淘宝视觉合规巡检系统"的继续开发。请先阅读项目内的 `HANDOVER.md`、进度表 `淘宝巡检系统_项目进度_20260821.xlsx`，以及 `app/` 下的源码（FastAPI + SQLite + Playwright 单页应用），再制定推进计划，**不要凭空假设、不要重复造轮子**。
>
> ## 一、项目是什么
> 一个对内 Web 系统：录入"官旗标准图"，对代理/分销商的淘宝商品页做巡店，自动抓取主图/SKU图/详情图，用 AI 视觉比对是否违规（价格篡改、违规文案、牛皮癣、盗用官旗图等），输出违规看板、状态流转、核销留痕、数据报表。
>
> ## 二、技术栈与代码地图
> - 后端主流程：`app/app.py`（FastAPI 路由 + 比对主流程）、`app/db.py`（SQLite 建表/设置读写）、`app/crawler.py`（Playwright 抓取 + 淘宝登录态）
> - 视觉比对：`app/vision_llm.py`（**新，主用**）OpenAI 兼容的 GPT-4V 语义比对；`app/compare.py`（**旧 SSIM/pHash 像素比对，仅作回退兜底**，不要再用来做主结论）
> - 前端：`app/index.html`（SPA，含对比视图 / 违规看板 / 数据报表 / 设置页）
> - 部署：`app/Dockerfile`、`Procfile`、`railway.json`、`render.yaml`、`app/DEPLOY.md`
> - 交接/进度：`HANDOVER.md`、根目录进度表 xlsx
>
> ## 三、当前进度（截至 2026-08-21，整体加权约 84%，但核心四项未达标）
> 已完成（100%）：店铺管理 / 视觉标准库 / 巡检任务（单链接+Excel批量）/ 状态流转(四态) / 核销凭证+审计 / 数据报表 / Git 本地仓库 / 部署配置框架。
> 严重问题（接手后优先处理，均为 P0）：
> 1. **图片抓取准确性 40%** —— Playwright 抓取主图/SKU/详情的分类与召回不准，是整个系统可信度的根因阻塞。
> 2. **AI 视觉比对 60%** —— 已重构为 GPT-4V 语义比对，但原开发沙箱无外网未做真实回归；需填 API Key + 联网验证才能验收。
> 3. **网站稳定性 50%** —— 重复实例抢库导致 `readonly database`、崩溃无自恢复。
> 4. **操作面板 / UI 70%** —— 功能可用但交互体验未达验收标准。
>
> ## 四、设备变更特别提醒（新同事机器与原机不同）
> - **代码来源**：原仓库**尚未推送远程**。需先从原机器拷贝整个项目目录，或让原作者 push 后再 clone；不要假设代码已在你机器上。
> - **环境重装**：Python 3.13 → 建 venv → `pip install -r app/requirements.txt` → `playwright install chromium`。
> - **重新登录淘宝**：`app/auth.json` 含原机真实登录态，**不跨设备、不可复用**，必须在新机用网站"扫码登录"重新生成。
> - **重新配 Key**：视觉模型 Key 走环境变量 `VISION_API_KEY`（参考 `.env.example`，真正写在项目根 `.env`，已被 gitignore），**绝不写进代码或提交**。
> - **路径**：全程用项目相对路径，不要依赖原机绝对路径（如 `C:\Users\...`）。
> - **运行**：单实例启动（已有防重复启动锁逻辑），避免多进程抢 SQLite 导致 readonly。
>
> ## 五、请你先做的（按顺序）
> 1. 在本机把项目跑起来：建 venv → 装依赖 → `playwright install chromium` → 重新扫码登录 → `python -m uvicorn app:app --host 0.0.0.0 --port 8000`（或 `pythonw app.py`），打开 `/docs` 自检接口连通。
> 2. 填好 `VISION_API_KEY`，在网站"项目设置"启用视觉模型，跑一次 `/api/vision/self-test` 验证 GPT-4V 连通；再对一条真实链接做端到端巡检，把 AI 比对从 60% 推进到可验收。
> 3. 优先攻坚图片抓取准确性（P0 第一阻塞）：定位主图/SKU/详情选择器与召回不准的根因，建立准确率验收基准后再回归。
> 4. 顺带处理网站稳定性（单实例锁 + 崩溃自恢复）与操作面板体验优化。
> 5. 完成后把本地 Git 推送到远程（原作者给地址后执行：`git remote add origin <地址> && git push -u origin master`）。
>
> 请先输出你的接手计划（含环境搭建步骤与优先级），再动手。遇到阻塞先排查现有代码与文档，不要重复造轮子。

---

**使用方式**：把上方引用块内全部文字复制 → 粘贴到新 AI 会话 → 发送即可。AI 会先给出接手计划，再按优先级推进。
