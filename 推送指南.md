# 把项目推到 GitHub（全网公开）— 傻瓜指南

## 为什么是你在自己电脑上操作
AI 运行环境没有外网，没法帮你 `git push`。但代码就在你电脑上的这个文件夹：
`C:\Users\J\WorkBuddy\2026-08-17-08-58-49`
远程地址我已经帮你配好了（origin = 你的 GitHub 仓库）。你本机有网，直接推即可，**不用再配任何东西**。

---

> ⚠️ **你目前的情况**：我已检查，你电脑**没装 Git、也没装 GitHub Desktop**。所以右键不会有「在终端中打开」菜单，打开终端也跑不了 `git` 命令。
> **请直接用下面的「方案 B（GitHub Desktop）」**——它是图形界面、自动带 git、用网页登录授权，全程不碰命令行、不需要记 token。

## 方案 B（首选）：GitHub Desktop —— 不敲命令、不记 token

1. 下载安装 GitHub Desktop：https://desktop.github.com （一路点下一步）
2. 打开它，用你的 GitHub 账号登录（会弹网页，点 **Allow** 授权即可）
3. 顶部菜单 `File` → `Add Local Repository` → 选文件夹 `C:\Users\J\WorkBuddy\2026-08-17-08-58-49` → 点 `Add`
4. 看到 `Publish repository` 按钮后：
   - Repository name 保持 `aupu1993`
   - **要公开就别勾 `Keep this code private`**；想私有就勾上
   - 点 `Publish Repository`
5. 进度走完 → 打开 https://github.com/taijing-613/aupu1993 就能看到代码了

> 之后每次改完代码：写个摘要 → 点 `Commit to master` → 再点 `Push origin` 即可。

---

## 方案 A：命令行（前提是你已装 Git for Windows）

> 只有想用命令行时才需要。先去 https://git-scm.com 安装 Git（一路下一步），装完才有 `git` 命令和「终端中打开」菜单。

1. 打开文件夹 `C:\Users\J\WorkBuddy\2026-08-17-08-58-49`
2. 打开终端（二选一）：
   - **地址栏法（最稳，不需要右键菜单）**：点文件夹顶部的**地址栏**，删掉里面路径，输入 `powershell` 或 `cmd`，回车 → 终端直接在该目录打开
   - 或空白处 **Shift + 右键** →「在终端中打开」/「在此处打开 PowerShell 窗口」
3. 输入这一行，回车：
   ```
   git push -u origin master
   ```
4. 如果弹出登录框：
   - **用户名**：填你的 GitHub 用户名
   - **密码**：填「个人访问令牌(token)」，**不是**你的 GitHub 登录密码（GitHub 早就不允许用密码推送了）

> 如果提示 `git 不是内部或外部命令`，说明你没装 Git：去 https://git-scm.com 下载安装，一路下一步，装完重开终端再试。

---

## 怎么拿到「个人访问令牌(token)」（密码已失效，仅命令行方案需要）

1. 登录 github.com → 右上角头像 → **Settings**
2. 左侧最下面 **Developer settings** → **Personal access tokens** → **Tokens (classic)**
3. 点 **Generate new token (classic)**
4. Note 随便写（如 `push`）；Expiration 选 90 days；**勾选 `repo`（整组都勾上）**
5. 拉到底点 **Generate** → **复制**那串 `ghp_...` 的 token（只显示这一次！）
6. 回到 push 时的密码框，粘贴进去

---

## 确认「全网公开」真的生效

去 https://github.com/taijing-613/aupu1993
- 如果页面显示 **Private**（私有），别人看不到：
  - 点 **Settings** → **Change visibility** → 选 **Public**
- 显示 **Public** 就对了，谁都能看你的代码

---

## 下一步：让网站也能在外网打开（部署公网）

> 注意：**代码公开 ≠ 网站能外网访问**。现在网站还只能在你本机和局域网打开。
> 要让异地同事直接点链接就用，需要部署一次：

1. 登录 https://railway.app （用 GitHub 账号登录）
2. **New Project** → **Deploy from GitHub repo** → 选 `aupu1993`
3. 部署配置我已经帮你备好（railway.json / render.yaml），Railway 会自动识别，等 1~2 分钟
4. 完成后会给你一个 `https://xxx.railway.app` 的公网地址，发给同事即可用

（Railway 这一步如果卡住，把页面截图发我，我帮你看着点。）

---

## 遇到问题对照表

| 报错 | 原因 | 解决 |
|---|---|---|
| `Could not resolve host / 超时` | 你本机也没网 | 检查网络，或换有网的环境 |
| `403 / 404` | 仓库名不对或没权限 | 核对地址，确认是你本人仓库 |
| 一直要密码且拒绝 | 用了登录密码 | 改用上面生成的 token（命令行方案） |
| `src refspec master does not match` | 本地分支名不同 | 用 `git branch` 看分支名，把命令里 master 换成实际分支名 |
| 右键没有「在终端中打开」 | 没装 Git / 系统版本 | 用「地址栏输入 cmd」法，或改用方案 B（Desktop） |
