# 淘宝视觉合规巡检系统 — 使用手册与设计说明

> 适用对象：使用方 + 接手开发的同事
> 数据截至：2026-08-21（整体加权完成度约 84%，核心四项未达标，详见第五节）
> 配套文档：HANDOVER.md（交接）、DEPLOY.md（部署）、淘宝巡检系统_项目进度_20260821.xlsx（进度表）

---

## 一、系统概述

这是一个**对内**使用的「AI 电商视觉合规巡检系统」。核心目标：让品牌方对**代理 / 分销商**的淘宝商品页做"巡店"，自动核对它们是否严格遵循**官旗视觉标准**——有没有价格篡改、违规文案、牛皮癣广告、盗用官旗主图等。

工作流一句话：**录入官旗标准图 → 给定「代理商链接 + 官旗标准链接」一对对照 → 系统自动抓取两侧主图 / SKU 图 / 详情图 → AI 视觉比对判定违规 → 输出违规看板、状态流转、核销留痕、数据报表。**

---

## 二、网站使用方法（面向使用者）

### 2.1 进入与身份登记
- 打开网站（本地 `http://127.0.0.1:8000`，部署后为公网 URL），**首次打开会弹出门禁**，填写「姓名 + 部门」即可登记为成员，**无需独立账号**。
- 顶部提供「扫码登录淘宝 / 天猫」入口：点开后系统弹出浏览器窗口，手机淘宝扫码一次，登录态自动保存到本机，之后抓取淘宝 / 天猫商品页就**不用再手动填 Cookie**。Cookie 过期后重新扫码一次即可。

### 2.2 导航（左侧栏，支持快捷键）
| 页面 | 快捷键 | 作用 |
|---|---|---|
| 仪表盘 | ⌘1 | 总览：店铺数 / 待处理违规 / 平均合规率 / 已修改，含团队动态时间线 |
| 店铺管理 | ⌘2 | 录入 / 编辑代理、分销商店铺（名称、负责人、链接、头像） |
| SKU 映射 | ⌘3 | 官旗 SKU 与代理 SKU 的对应关系维护 |
| 视觉标准库 | ⌘4 | 录入**官旗标准图**（命名、页型、基准图），作为合规判定基准 |
| 违规看板 | ⌘5 | 发起巡检 + 查看结果 + 状态流转（核心工作页） |
| 数据报表 | ⌘6 | 巡检量 / 状态分布 / 合格率 / 趋势 / Top 店铺，可导出 CSV |
| 项目设置 | — | 阈值、通知模板、**视觉模型启用与 API 配置** |
| 成员 | — | 门禁登记的成员列表 |

### 2.3 发起一次巡检（在「违规看板」页）
**（A）单链接巡检（精准对照）**
1. 填「代理商链接」+「官旗标准链接」。
2. 「图片类型」选填：全部（主图+SKU+详情）/ 仅主图 / 仅 SKU / 仅详情。
3. 抓取淘宝 / 天猫商品页**必填 Cookie**（除非已扫码登录）：从浏览器开发者工具复制 `k=v; k2=v2…`；已扫码则留空。
4. 点「开始单链接巡检」→ 进入队列、自动抓取 + 比对。

**（B）扫码免 Cookie**（推荐）
- 在「淘宝 / 天猫登录」面板点「扫码登录淘宝 / 天猫」→ 手机扫码 → 状态变「已登录」，此后 A 步骤的 Cookie 框可留空。

**（C）批量导入（精准对照表）**
1. 点「下载对照表模板」→ 按列填「代理商链接 / 官旗标准链接 / 图片类型」。
2. 点「选择 Excel 上传」→ 系统为每一行创建一条单链接巡检。

### 2.4 查看结果与对比视图
- 仪表盘看全局概览；违规看板**按店铺分组**展示巡检卡片。
- 点开卡片展开「左右对比视图」：**左侧官旗图、右侧代理图**，分主图 / SKU / 详情三类，标注差异与缺失（代理缺图、错位、AI 判定违规等）。
- 若已启用视觉模型，每类下方显示 **🤖 AI 判定结论**（违规 / 一致 / 存疑 + 违规类型标签 + 理由）；模型不可用时显示像素比对兜底结论。

### 2.5 状态流转（四态闭环）
`待处理 pending` → `已通知 notified` → `已修改 modified` → `已核销 verified`

- 在违规看板勾选多条 →「批量通知选中」→ 发整改通知并置为已通知。
- 代理整改完成 → 置为已修改。
- 整改验收 → 点「核销」，**上传凭证截图 + 备注** → 进入已核销（闭环）。凭证落盘 `uploads/evidence`，操作写入审计日志 `activity_log`。
- 误核销可「撤回」回到已修改。

### 2.6 数据报表与导出
- 数据报表页展示：巡检总量、整改合格率、待处理违规、已核销、差异命中、处理状态分布、抓取状态分布、近 14 天趋势、Top 店铺。
- 点「导出 CSV」下载全量巡检明细（含核销留痕字段）。

### 2.7 项目设置
- 视觉相似度阈值、价格容差、整改通知邮件模板（支持 `{shop}/{sku}/{type}/{detail}` 变量）。
- **视觉模型配置**（新增）：启用开关、API Base URL、模型名。填好 Key 后点「视觉自检」验证连通。

---

## 三、设计的逻辑（架构与原理）

### 3.1 技术栈
- **后端**：FastAPI（REST + SSE 实时推送）
- **持久化**：SQLite（WAL 模式支持并发读写，`busy_timeout` 防锁死）
- **抓取引擎**：Playwright（无头浏览器 + 本机 Edge + 反自动化绕过）+ 扫码登录态 `auth.json`
- **前端**：单页应用 `app/index.html`，原生 JS，无前端框架；SSE 驱动实时更新
- **比对引擎**：多模态大模型（GPT-4V 语义比对）为主，传统像素算法为兜底

### 3.2 代码地图
| 文件 | 职责 |
|---|---|
| `app/app.py` | 全部 API 路由、巡检主流程编排、设置读写、视觉自检 |
| `app/db.py` | SQLite 建表、连接（WAL）、设置与成员读写 |
| `app/crawler.py` | Playwright 抓取主图 / SKU / 详情、淘宝登录态检测与扫码 |
| `app/vision_llm.py` | **视觉大模型比对（主）**：官旗图+代理图→结构化违规判定 JSON |
| `app/compare.py` | **像素比对（兜底）**：SSIM + pHash，模型不可用时回退 |
| `app/index.html` | SPA 前端：仪表盘 / 店铺 / 标准库 / 违规看板 / 报表 / 设置 / 成员 |
| `app/Dockerfile` `Procfile` `railway.json` `render.yaml` `DEPLOY.md` | 部署配置（Docker / Railway / Render） |

### 3.3 核心数据模型（db.py）
- `shops` 店铺、`sku` SKU 映射、`standards` 视觉标准库
- `inspections` 巡检主表：含 `agent_url`/`official_url`/`image_type`/`state`/`status`、`off_*`(官旗抓取结果)/`agent_*`(代理抓取结果)、`vision_*`(AI 结论)、`verified_*`(核销留痕)
- `activity_log` 审计留痕、`members` 门禁成员、`settings` 设置

### 3.4 比对引擎"双轨制"
- **主轨 `vision_llm.py`**：把「官旗图 + 代理图 + 违规判定提示词」发给 OpenAI 兼容视觉模型（默认 GPT-4V / gpt-4o），返回结构化 JSON（是否违规 / 违规类型 / 置信度 / 理由），能识别**价格篡改、违规文案、牛皮癣、盗图**等语义级违规。
- **兜底轨 `compare.py`**：SSIM + pHash 像素相似度。仅当模型**未启用 / 无 Key / 无网 / 调用异常**时自动回退，并在前端明确标注"像素级兜底，结论仅供参考"。
- 设计原则：**模型不可用绝不阻断巡检**，但会诚实标注结论置信度。

### 3.5 实时协作与稳定性
- SSE（`/api/events`）推送团队动态、在线状态、巡检状态变化；违规看板外壳只构建一次、SSE 仅刷新列表，避免文件上传控件被重渲染导致崩溃。
- 单实例锁防止重复进程抢 SQLite（历史 bug：`readonly database`）。
- 密钥安全：`auth.json`（登录态）、`.env`（`VISION_API_KEY`）均被 `.gitignore` 排除，不进代码、不进仓库。

---

## 四、操作的逻辑（交互与状态机）

### 4.1 两条状态轴
- **执行轴 `inspection.state`**：`queued 队列中` → `running 抓取中` → `done 已完成` / `error 失败`。
- **业务轴 `inspection.status`**：`pending 待处理` → `notified 已通知` → `modified 已修改` → `verified 已核销`。

### 4.2 流转触发
- 批量 / 单条通知 → `notified`
- 代理整改 → `modified`
- 上传凭证核销 → `verified`（需先到 `modified`，不可跨越）
- 撤回 reopen → 回到 `modified`

### 4.3 对比视图内的操作按钮
- **重抓 (recrawl)**：重新抓取两侧图片
- **上传复核图 (recheck)**：无法自动抓取时人工补图
- **核销 (verify)**：上传凭证 + 备注，闭环
- **撤回 (reopen)**：核销后回退

### 4.4 看板组织
- 卡片按 `shop_host`（代理域名）分组，可折叠；
- 顶部状态段控：`全部 / 待处理 / 已通知 / 已修改 / 已核销` 筛选；
- 勾选 +「批量通知选中」→ 批量发整改通知。

---

## 五、接下来推进方向（优先级）

### P0 — 必须优先
1. **图片抓取准确性（当前 40%）**：这是整套系统可信度的根。优先定位主图 / SKU / 详情「分类不准、召回不全」的根因（反爬、页面模板变化、Cookie 时效、选择器脆弱），并建立**准确率验收基准**后再谈功能完整性。
2. **AI 视觉比对真实回归（当前 60%）**：当前比对引擎已重构为 GPT-4V 语义比对，但原开发沙箱无外网、未做真实端到端验证。需：填 `VISION_API_KEY` → 网站设置启用视觉模型 → 跑 `/api/vision/self-test` → 对真实链接端到端巡检，把准确率从 60% 推进到可验收；并评估是否需"逐张比对"（当前只比代表图以控成本）。
3. **网站稳定性（当前 50%）**：治理重复实例抢库、增加崩溃自恢复与平台保活，做稳定性压测，达成"所有人可稳定访问"。
4. **操作面板 / UI（当前 70%）**：梳理高频操作路径，做交互 / 布局重构，设定体验验收标准后再判定完成。

### P1 — 让项目可交付
5. **推送至 Git 远程 + 公网部署**：`git remote add origin <地址> && git push -u origin master`；用已备好的 Railway / Render 配置部署，取得公网 URL（CloudStudio 仅支持静态，本项目需 Python 托管）。
6. **多模态判定质量调优**：根据真实回归结果微调提示词、违规类型枚举、置信度阈值，降低误报 / 漏报。

### P2 — 增强
7. **反爬极端情形**：滑块验证 / IP 限流目前靠"过期重扫"人工兜底；触发频次高时引入代理 / 打码服务。
8. **逐张比对 / 报告导出增强**：支持单商品多图逐张判定、生成可发送的整改报告 PDF。

---

## 附：本地快速跑起来（接手用）
```bash
cd app
python -m venv venv && source venv/bin/activate   # Windows: venv\Scripts\activate
pip install -r requirements.txt
playwright install chromium
# 在网站「淘宝/天猫登录」面板扫码登录（生成 auth.json）
# 在项目根 .env 写入 VISION_API_KEY=sk-xxx（可选，启用 AI 比对用）
python -m uvicorn app:app --host 0.0.0.0 --port 8000
# 浏览器打开 http://127.0.0.1:8000  →  /docs 查看全部 API
```
